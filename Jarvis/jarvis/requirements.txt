# Jarvis voice assistant — pinned dependencies (Python 3.11, Windows)

# Speech-to-text
faster-whisper==1.1.1

# Text-to-speech
edge-tts==7.2.8

# Audio capture (microphone) + numeric processing
sounddevice==0.5.1
numpy==1.26.4
scipy==1.17.1              # resample mic audio (native rate -> 16 kHz)

# Audio playback for edge-tts mp3 output
pygame==2.6.1

# Wake-word detection ("hey jarvis")
openwakeword==0.6.0
onnxruntime==1.20.1

# LLM backends
anthropic==0.49.0          # Claude (claude-opus-4-8)
openai==1.66.3             # MiniMax M3 (OpenAI-compatible endpoint)

# PC control ("hands" — Phase 2)
pyautogui==0.9.54          # typing, volume keys, screenshots
pywhatkit==5.4             # play YouTube videos by search
pygetwindow==0.0.9         # window handling for PC control

# Accountability mentor store (Phase 3)
supabase==2.9.1            # commitments / facts / check_ins tables

# Scheduled background automation (Phase 4)
apscheduler==3.11.0        # BackgroundScheduler for timed briefings / nudges

# Phone integration (Phase 5) — Telegram bot, long-polling (no public URL needed)
pyTelegramBotAPI==4.34.0   # import name: telebot

# Config / .env loading
python-dotenv==1.0.1
